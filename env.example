# Local (SQLite) — use with prisma/schema.sqlite.prisma
# DATABASE_URL="file:./prisma/dev.db"

# Production (Vercel Postgres) — used by prisma/schema.prisma
# POSTGRES_PRISMA_URL="postgres://..."
# POSTGRES_URL_NON_POOLING="postgres://..."

